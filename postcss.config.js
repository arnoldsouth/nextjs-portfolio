// Out of the box, with no configuration, Next.js compiles CSS using PostCSS
// customize PostCSS config by creating this file at top-level.
// useful if you're using libraries like Tailwind CSS

module.exports = {
  plugins: {
    tailwindcss: {},
    autoprefixer: {},
  },
};
